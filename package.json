{
  "name": "tg-api-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — proxy requests to Telegram Bot API",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
